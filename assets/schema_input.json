{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nfdata-omics/nanotranseq/master/assets/schema_input.json",
    "title": "nfdata-omics/nanotranseq pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "replicate": {
                "type": "integer",
                "errorMessage": "Replicate must be an integer",
                "meta": ["replicate"]
            },
            "barcode": {
                "type": "string",
                "errorMessage": "Barcode must be provided if applicable",
                "meta": ["barcode"]
            },
            "fastq_1": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.f(ast)?q\\.gz$",
                "errorMessage": "FastQ file for reads 1 must be provided, cannot contain spaces and must have extension '.fq.gz' or '.fastq.gz'"
            },
            "condition": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Condition must be provided and cannot contain spaces",
                "meta": ["condition"]
            },
            "fastq_2": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.f(ast)?q\\.gz$",
                "errorMessage": "FastQ file for reads 2 cannot contain spaces and must have extension '.fq.gz' or '.fastq.gz'"
            },
            "fast5": {
                "type": "string",
                "format": "path",
                "exists": true,
                "errorMessage": "fast5 must be a path to an existing directory of raw FAST5/POD5 signal"
            }
        },
        "required": ["sample", "fastq_1", "condition"]
    }
}
