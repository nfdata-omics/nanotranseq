---
# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/modules/environment-schema.json
channels:
  - conda-forge
  - bioconda
dependencies:
  # tximport oarfish support requires >= 1.32 (Bioconductor 3.20 ships tximeta 1.24 / tximport 1.34)
  - bioconda::bioconductor-tximeta=1.24.0
